pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh "Running under $STAGE_NAME"
            }
        }
        stage('Test') {
            steps {
                sh "Running under $STAGE_NAME"
            }
        }
        stage('Deliver') {
            steps {
                sh "Running under $STAGE_NAME"
            }
        }
    }      
}